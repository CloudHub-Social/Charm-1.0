---
'charm': patch
---

Replace the old active-client notification scope with an explicit desktop-delay mode so notification lease behavior, delay duration, and notify-all semantics stay predictable across devices.
