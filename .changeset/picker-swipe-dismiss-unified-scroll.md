---
default: patch
---

Fix the emoji/sticker picker on mobile: the sheet can now be dismissed by swiping/dragging its handle down, and the category sidebar now scrolls together with the picker content instead of in its own separate scrollbar.
