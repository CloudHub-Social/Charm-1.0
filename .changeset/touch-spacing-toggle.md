---
default: patch
---

Add a "Touch Spacing" toggle to Settings > General > Accessibility (on by default) so users who don't need the larger WCAG-minimum touch targets can opt into a more compact layout for the room header, composer, call, and image viewer buttons.
