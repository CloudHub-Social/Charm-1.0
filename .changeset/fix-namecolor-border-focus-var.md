---
default: patch
---

Fix a missing `--sable-border-focus` CSS variable that left the account color-picker swatch's idle-state border invisible; it now shows a subtle theme-adaptive border matching the app's primary accent.
