---
'charm': patch
---

Preserve encrypted message notification lifecycle state from event arrival through decrypt replay so background and foreground delivery stay consistent when notification policy resolves after decryption.
