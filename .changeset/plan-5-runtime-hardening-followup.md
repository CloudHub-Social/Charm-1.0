---
default: patch
---

Avoid false-positive crypto-store recovery reloads, surface service worker updates that swap the active worker immediately, and retry temporarily blocked media once the cooldown expires.
