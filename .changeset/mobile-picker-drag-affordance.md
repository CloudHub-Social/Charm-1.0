---
default: patch
---

Fix the mobile emoji/GIF/sticker picker's drag handle being nearly invisible (low-contrast color token) and give the sheet a proper close animation instead of an instant, sometimes-jumpy dismissal.
