---
default: patch
---

Restore vertical padding and emoji-offset CSS variables on jumbo emoji message lines, so consecutive jumbo-emoji messages from the same sender no longer render with their line boxes touching.
